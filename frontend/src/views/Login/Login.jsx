import React from "react";

import UserLogin from "../../components/LoginForm/UserLogin";

const Login = (props) => {
    return (
        <div>
            <header>
                <h1>Login</h1>
            </header>
            <UserLogin/>
        </div>
    );
};

export default Login;
